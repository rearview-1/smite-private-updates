Signed incremental update from 0.3.3: 10 changed files, 100,410 payload bytes.

- Includes owned-host shutdown and repeat-match lifecycle repairs.
- Starts the existing pedestal presentation observer during AWS selection and retains bounded public diagnostics.
- Expands read-only Old Loki audio diagnostics; this is not a completed sound repair.

Close SMITE and wait for rollback, then run CHECK FOR UPDATES.bat from your 0.3.3 installation. Startup also checks this signed channel. No GitHub account is required.

The updater preserves profile/account/enrollment data. Game content, bundled runtime and recovery bootstrap are unchanged. Existing 0.3.1/0.3.2 installations need the separately distributed 0.3.3 initial package first.

The rejected Vanish-laugh removal candidate is excluded. Native pedestal/audio and two-player gameplay acceptance remain pending. Publishing this channel does not deploy a new AWS host snapshot.

Validation: 113 tests passed, plus full recipient setup, preparation, repair,
support and relocation checks. The packaged updater downloaded and applied this
release from public GitHub without credentials, verified all final hashes and
preserved synthetic user data. No game was launched by these checks.
